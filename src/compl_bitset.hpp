#ifndef AUTOMATA_COMPL_BITSET_HPP
#define AUTOMATA_COMPL_BITSET_HPP

#include <memory>

namespace automata {

	template <typename Block = size_t, typename Allocator = std::allocator<Block>>
	struct compl_bitset {

	};
}
#endif // #ifndef AUTOMATA_COMPL_BITSET_HPP
